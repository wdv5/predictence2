# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0

# ── Data / time-series ────────────────────────────────────────────────────────
pandas>=2.2.2
prophet>=1.1.5
plotly>=5.24.1           # suppresses Prophet interactive-plot warning

# ── Phase 2: Machine Learning ─────────────────────────────────────────────────
scikit-learn>=1.4.0      # IsolationForest + StandardScaler
numpy>=1.26.0

# ── Phase 2: Live Prometheus scraping ─────────────────────────────────────────
prometheus-api-client>=0.5.5

# ── Phase 2: Persistent state store ───────────────────────────────────────────
psycopg2-binary>=2.9.9   # TimescaleDB / PostgreSQL driver (sync)
redis>=5.0.4             # Redis client
